<Window
    x:Class="WmsLayer.Portal.Demo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013"
    xmlns:local="clr-namespace:WmsLayer.Portal.Demo"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.Resources>
        <local:MapViewModel x:Key="MapViewModel" />
    </Window.Resources>
    <Grid>
        <esri:MapView Map="{Binding Map, Source={StaticResource MapViewModel}}" />
        <Label
            Padding="10"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Background="Red"
            Content="{Binding MapTitle, Source={StaticResource MapViewModel}}"
            FontSize="30"
            Foreground="White" />
    </Grid>
</Window>
